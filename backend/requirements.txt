# ==============================================================
# backend/requirements.txt
# Owner: Developer 2 (Backend / Simulation)
# NOTE: No LLM SDK needed here — all LLM calls are made inside the n8n
#       Groq AI Agent node. This backend is a pure CRUD / data API.
# ==============================================================

fastapi==0.115.0
uvicorn[standard]==0.30.6
pymongo==4.9.0
pydantic==2.9.2
pydantic[email]>=2.0.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2

# PDF report generation
fpdf2==2.7.9

# Email validation (required by pydantic EmailStr in app/schemas/config.py)
email-validator>=2.0.0

# Authentication & Security
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
motor>=3.0.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
mongomock==4.3.0
